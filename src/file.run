reset;
model file.mod;
data Dati/file.dat;
option solver gurobi;
solve;

display TurniMinimi; 
display u, DPT, St, Mt; #y, x, z, Boost; #solo se si vogliono vedere le caratteristiche per ogni personaggio
